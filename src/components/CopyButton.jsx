import{useState}from'react';import{Copy,Check}from'lucide-react';import{supabase}from'../lib/supabase';import{useToast}from'./Toast';export default function CopyButton({prompt,id}){const[c,setC]=useState(false),toast=useToast();const go=async e=>{e.preventDefault();try{await navigator.clipboard.writeText(prompt)}catch{toast('Unable to copy the prompt.');return}if(supabase&&id){const{data:{user}}=await supabase.auth.getUser();if(user)await supabase.rpc('increment_copy_count',{prompt_id:id})}setC(true);toast('Prompt copied!');setTimeout(()=>setC(false),1500)};return <button className="button copy" onClick={go}>{c?<Check size={15}/>:<Copy size={15}/>} {c?'Copied!':'Copy Prompt'}</button>}
