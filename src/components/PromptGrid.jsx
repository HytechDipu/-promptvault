import PromptCard from'./PromptCard';export function LoadingState(){return <div className="grid">{[1,2,3].map(i=><div className="skeleton" key={i}/>)}</div>}export function EmptyState({children='Nothing here yet.'}){return <div className="empty">{children}</div>}export default function PromptGrid({items,data,loading,error,trending}){const prompts=items||data||[];if(loading)return <LoadingState/>;if(error)return <div className="error">{error}</div>;if(!prompts.length)return <EmptyState/>;return <div className="grid">{prompts.map(i=><PromptCard key={i.id} item={i} showTrend={trending}/>)}</div>}
