import{Search}from'lucide-react';export default function SearchBar({value,onChange,onSubmit}){return <form className="search" role="search" onSubmit={e=>{e.preventDefault();onSubmit?.()}}><Search size={20} aria-hidden="true"/><label className="sr-only" htmlFor="prompt-search">Search the prompt library</label><input id="prompt-search" value={value} onChange={e=>onChange(e.target.value)} placeholder="Search prompts, styles, categories..."/></form>}
