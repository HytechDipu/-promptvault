import{Link}from'react-router-dom';import{formatCount}from'../utils/helpers';import CopyButton from'./CopyButton';import LikeButton from'./LikeButton';export default function PromptCard({item,showTrend=false}){return <article className="card"><Link to={`/prompt/${item.id}`} className="image-wrap"><img loading="lazy" src={item.image_url} alt={item.title}/>{showTrend&&<span className="trend">Trending</span>}</Link><div className="card-body"><div className="meta"><span>{item.category}</span><span>{formatCount(item.copy_count)} copies</span></div><Link to={`/prompt/${item.id}`}><h3>{item.title}</h3></Link><p>{item.prompt}</p><div className="card-actions"><CopyButton prompt={item.prompt} id={item.id}/><LikeButton id={item.id}/></div></div></article>}
